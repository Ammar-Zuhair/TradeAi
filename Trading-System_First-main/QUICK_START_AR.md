# خطوات سريعة - البدء مع Docker 🚀

## المطلوب منك الآن:

### 1️⃣ إنشاء ملف .env (دقيقة واحدة)

افتح PowerShell في مجلد المشروع واكتب:

```powershell
# نسخ الملف النموذجي
Copy-Item .env.example .env

# فتح الملف للتعديل
notepad .env
```

**ضع بياناتك الحقيقية:**
```env
MT5_LOGIN=12345678           # رقم حسابك
MT5_PASSWORD=YourPassword    # كلمة المرور
MT5_SERVER=YourBroker-Server # مثال: MetaQuotes-Demo
ENABLE_AUTO_TRADING=false    # true للتداول الآلي
RISK_PERCENTAGE=1.0
```

احفظ الملف (Ctrl+S) وأغلقه.

---

### 2️⃣ تعديل docker-compose.yml (30 ثانية)

افتح ملف `docker-compose.yml` واستبدل `yourusername` باسم مستخدمك في Docker Hub:

```yaml
# قبل:
image: yourusername/mt5-trading-system:latest

# بعد (مثال):
image: ahmed123/mt5-trading-system:latest
```

احفظ الملف.

---

### 3️⃣ التأكد من تثبيت Docker (إذا لم يكن مثبتاً)

```powershell
# تحقق من وجود Docker
docker --version

# إذا لم يكن مثبتاً:
# حمّل من: https://www.docker.com/products/docker-desktop
# ثبّت Docker Desktop for Windows
# تأكد من تفعيل Windows Containers
```

---

### 4️⃣ بناء Docker Image (10-20 دقيقة)

```powershell
# في مجلد المشروع
cd "C:\Users\USERW\Desktop\Best_Models_Graduation_Project\Final_project"

# بناء الصورة
docker-compose build
```

**انتظر حتى ينتهي البناء...**

---

### 5️⃣ اختبار محلياً (اختياري)

```powershell
# تشغيل النظام
docker-compose up

# شاهد اللوجات للتأكد من عمل MT5
# اضغط Ctrl+C للإيقاف
```

---

### 6️⃣ تسجيل دخول Docker Hub

إذا لم يكن لديك حساب:
1. اذهب إلى https://hub.docker.com
2. سجّل حساب جديد (مجاني)
3. احفظ اسم المستخدم وكلمة المرور

```powershell
# تسجيل دخول
docker login

# أدخل اسم المستخدم: ______
# أدخل كلمة المرور: ______
```

---

### 7️⃣ رفع الصورة إلى Docker Hub (5-15 دقيقة)

```powershell
# رفع الصورة
docker-compose push

# انتظر حتى ينتهي الرفع...
```

---

### ✅ تم! الآن لديك Docker Image على Docker Hub

للتحقق:
1. اذهب إلى https://hub.docker.com
2. سجّل دخول
3. ستجد الصورة في قسم **Repositories**

---

## 🔄 للانتقال إلى استضافة جديدة:

### على الاستضافة الجديدة:

```powershell
# 1. إنشاء مجلد
mkdir C:\TradingSystem
cd C:\TradingSystem

# 2. إنشاء docker-compose.yml
# انسخ محتوى ملف docker-compose.yml الخاص بك

# 3. إنشاء .env
notepad .env
# ضع نفس البيانات السابقة

# 4. تسجيل دخول Docker
docker login

# 5. سحب الصورة
docker-compose pull

# 6. تشغيل النظام
docker-compose up -d

# 7. مشاهدة اللوجات
docker-compose logs -f
```

**الأمر كله يأخذ 3-5 دقائق فقط! 🎉**

---

## 📚 للمزيد من التفاصيل:

راجع الدليل الشامل:
- **[DOCKER_DEPLOYMENT_GUIDE_AR.md](./DOCKER_DEPLOYMENT_GUIDE_AR.md)** - دليل كامل ومفصّل

---

## ❓ إذا واجهت مشكلة:

### المشكلة: .env file not found
- **الحل:** تأكد من إنشاء ملف `.env` في نفس مجلد المشروع

### المشكلة: MT5 initialization failed
- **الحل:** تأكد من صحة بيانات الاعتماد في `.env`

### المشكلة: Docker build failed
- **الحل:** تأكد من اتصال الإنترنت وحاول مرة أخرى

### المشكلة: Cannot push to Docker Hub
- **الحل:** تأكد من تسجيل الدخول (`docker login`) وصحة اسم المستخدم

---

## 🎯 الملخص:

1. ✅ أنشئ `.env` بالبيانات الحقيقية
2. ✅ عدّل `docker-compose.yml` (اسم المستخدم)
3. ✅ بناء: `docker-compose build`
4. ✅ رفع: `docker-compose push`
5. ✅ **انتهى! الآن يمكنك النشر على أي استضافة في دقائق**

---

**مبروك! 🎊 نظام التداول الخاص بك الآن محمول ومعزول وجاهز للنشر!**
