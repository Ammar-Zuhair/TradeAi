# MT5 Trading System - Quick Start 🚀

## نظام التداول الآلي باستخدام MetaTrader5

### 📁 الملفات الجديدة المُضافة

تم إنشاء البنية التحتية لـ Docker التالية:

| الملف | الوصف |
|-------|-------|
| `Dockerfile` | ملف بناء Docker Image |
| `docker-compose.yml` | إدارة الـ Containers |
| `requirements.txt` | المكتبات المطلوبة |
| `.env.example` | نموذج لملف البيئة |
| `.dockerignore` | استبعاد الملفات غير الضرورية |
| `env_loader.py` | تحميل بيانات الاعتماد |
| `startup.ps1` | سكريبت البدء |
| `DOCKER_DEPLOYMENT_GUIDE_AR.md` | **الدليل الشامل** 📖 |

---

## ⚡ البدء السريع

### 1️⃣ إنشاء ملف .env

```powershell
# نسخ الملف النموذجي
Copy-Item .env.example .env

# تعديل البيانات
notepad .env
```

**ضع بياناتك الحقيقية:**
```env
MT5_LOGIN=12345678
MT5_PASSWORD=YourPassword
MT5_SERVER=YourBroker-Server
ENABLE_AUTO_TRADING=false
```

### 2️⃣ بناء Docker Image

```powershell
# تعديل اسم المستخدم في docker-compose.yml أولاً
docker-compose build
```

### 3️⃣ اختبار محلياً

```powershell
docker-compose up
```

### 4️⃣ رفع إلى Docker Hub

```powershell
# تسجيل دخول
docker login

# رفع الصورة
docker-compose push
```

---

## 🔄 الانتقال لاستضافة جديدة

```powershell
# 1. سحب الصورة
docker-compose pull

# 2. إنشاء .env بنفس البيانات
notepad .env

# 3. تشغيل
docker-compose up -d
```

**الأمر كله يأخذ 5 دقائق فقط! ✅**

---

## 📚 الدليل الشامل

للتعليمات التفصيلية الكاملة، راجع:
👉 **[DOCKER_DEPLOYMENT_GUIDE_AR.md](./DOCKER_DEPLOYMENT_GUIDE_AR.md)**

---

## ⚙️ التعديلات على المشروع

تم تعديل `Run_System.py` ليقرأ بيانات الاعتماد من ملف `.env` بدلاً من hardcoding.

**قبل:**
```python
mt5.initialize()  # بدون بيانات اعتماد
```

**بعد:**
```python
from env_loader import Config
credentials = Config.get_mt5_credentials()
mt5.initialize(login=..., password=..., server=...)
```

---

## 🛠️ الأوامر المفيدة

```powershell
# مشاهدة اللوجات
docker-compose logs -f

# إيقاف النظام
docker-compose down

# إعادة التشغيل
docker-compose restart

# الدخول إلى الـ container
docker exec -it mt5-trading-bot powershell
```

---

## ⚠️ ملاحظات مهمة

1. ✅ **لا ترفع ملف `.env`** إلى Git أو Docker Hub (محمي تلقائياً)
2. ✅ **احتفظ بنسخة احتياطية** من `.env` في مكان آمن
3. ✅ **اختبر محلياً** قبل الرفع إلى Docker Hub
4. ✅ **راجع الدليل الشامل** للمزيد من التفاصيل

---

## 📞 الدعم

إذا واجهت أي مشكلة، راجع قسم "استكشاف الأخطاء" في الدليل الشامل.

---

**صُنع بـ ❤️ للتداول الآلي**
