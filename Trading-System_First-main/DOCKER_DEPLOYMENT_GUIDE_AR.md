# دليل النشر الشامل لنظام التداول باستخدام Docker
# Complete Deployment Guide for Trading System using Docker

<div dir="rtl">

## 📋 جدول المحتويات

1. [المتطلبات الأساسية](#المتطلبات-الأساسية)
2. [الإعداد الأولي](#الإعداد-الأولي)
3. [بناء Docker Image](#بناء-docker-image)
4. [رفع الصورة إلى Docker Hub](#رفع-الصورة-إلى-docker-hub)
5. [النشر على استضافة جديدة](#النشر-على-استضافة-جديدة)
6. [إدارة الـ Container](#إدارة-الـ-container)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🔧 المتطلبات الأساسية

### على جهازك الحالي:
- ✅ Windows 10/11 Pro أو Windows Server
- ✅ Docker Desktop for Windows (مع دعم Windows Containers)
- ✅ حساب على [Docker Hub](https://hub.docker.com) (مجاني)
- ✅ MetaTrader5 مثبت ويعمل
- ✅ بيانات اعتماد MT5 (رقم الحساب، كلمة المرور، السيرفر)

### على الاستضافة:
- ✅ Windows Server 2019 أو أحدث
- ✅ Docker for Windows Server مثبت
- ✅ اتصال بالإنترنت

---

## ⚙️ الإعداد الأولي

### الخطوة 1: تثبيت Docker Desktop

1. حمّل [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
2. شغّل المثبت واتبع التعليمات
3. بعد التثبيت، تأكد من تفعيل **Windows Containers**:
   ```powershell
   # التحقق من Docker
   docker --version
   docker info
   ```

### الخطوة 2: إنشاء حساب Docker Hub

1. اذهب إلى [hub.docker.com](https://hub.docker.com)
2. سجّل حساب جديد (مجاني)
3. احفظ اسم المستخدم وكلمة المرور

### الخطوة 3: تسجيل الدخول إلى Docker Hub

```powershell
# من PowerShell أو CMD
docker login
# أدخل اسم المستخدم وكلمة المرور
```

---

## 🏗️ بناء Docker Image

### الخطوة 1: إنشاء ملف .env

```powershell
# انسخ الملف النموذجي
Copy-Item .env.example .env

# افتح الملف وعدّل البيانات
notepad .env
```

**محتوى ملف .env (مثال):**
```env
MT5_LOGIN=12345678
MT5_PASSWORD=YourStrongPassword123
MT5_SERVER=MetaQuotes-Demo
ENABLE_AUTO_TRADING=true
RISK_PERCENTAGE=1.0
SYMBOL=XAUUSD
```

> ⚠️ **تحذير**: لا ترفع ملف `.env` إلى Docker Hub أو Git - البيانات حساسة!

### الخطوة 2: تعديل docker-compose.yml

افتح ملف `docker-compose.yml` وغيّر `yourusername` إلى اسم مستخدمك في Docker Hub:

```yaml
image: YOUR_DOCKERHUB_USERNAME/mt5-trading-system:latest
```

مثال:
```yaml
image: ahmed123/mt5-trading-system:latest
```

### الخطوة 3: بناء الصورة

```powershell
# تأكد من أنك في مجلد المشروع
cd "C:\Users\USERW\Desktop\Best_Models_Graduation_Project\Final_project"

# بناء الصورة
docker-compose build

# أو بدون docker-compose:
docker build -t YOUR_DOCKERHUB_USERNAME/mt5-trading-system:latest .
```

**الوقت المتوقع**: 10-20 دقيقة (حسب سرعة الإنترنت)

### الخطوة 4: اختبار الصورة محلياً

```powershell
# تشغيل الـ container
docker-compose up

# أو في الخلفية:
docker-compose up -d

# مشاهدة اللوجات:
docker-compose logs -f
```

---

## ☁️ رفع الصورة إلى Docker Hub

### الخطوة 1: رفع الصورة

```powershell
# رفع الصورة
docker-compose push

# أو بدون docker-compose:
docker push YOUR_DOCKERHUB_USERNAME/mt5-trading-system:latest
```

**الوقت المتوقع**: 5-15 دقيقة (حسب سرعة الإنترنت)

### الخطوة 2: التحقق من الرفع

1. اذهب إلى [hub.docker.com](https://hub.docker.com)
2. سجّل الدخول
3. ستجد الصورة في قسم **Repositories**

---

## 🚀 النشر على استضافة جديدة

### السيناريو: انتقلت إلى استضافة جديدة أو حدثت مشكلة

### الخطوة 1: تثبيت Docker على الاستضافة الجديدة

```powershell
# على Windows Server
# حمّل وثبّت Docker for Windows Server من الموقع الرسمي
```

### الخطوة 2: تسجيل الدخول إلى Docker Hub

```powershell
docker login
```

### الخطوة 3: إنشاء مجلد المشروع

```powershell
# إنشاء مجلد جديد
mkdir C:\TradingSystem
cd C:\TradingSystem
```

### الخطوة 4: إنشاء docker-compose.yml

انسخ محتوى `docker-compose.yml` أو حمّله من Docker Hub.

**يمكنك إنشاء ملف بسيط:**

```yaml
version: '3.8'

services:
  mt5-trading-system:
    image: YOUR_DOCKERHUB_USERNAME/mt5-trading-system:latest
    container_name: mt5-trading-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
```

### الخطوة 5: إنشاء ملف .env جديد

```powershell
# إنشاء ملف .env
notepad .env
```

**ضع نفس البيانات التي استخدمتها سابقاً:**
```env
MT5_LOGIN=12345678
MT5_PASSWORD=YourStrongPassword123
MT5_SERVER=MetaQuotes-Demo
ENABLE_AUTO_TRADING=true
RISK_PERCENTAGE=1.0
```

### الخطوة 6: سحب الصورة وتشغيلها

```powershell
# سحب أحدث نسخة
docker-compose pull

# تشغيل النظام
docker-compose up -d

# مشاهدة اللوجات
docker-compose logs -f
```

**✅ تم! النظام يعمل الآن على الاستضافة الجديدة**

---

## 🎮 إدارة الـ Container

### أوامر أساسية

```powershell
# مشاهدة الـ containers العاملة
docker ps

# إيقاف النظام
docker-compose down

# إعادة تشغيل النظام
docker-compose restart

# مشاهدة اللوجات
docker-compose logs -f

# الدخول إلى الـ container
docker exec -it mt5-trading-bot powershell

# مشاهدة استخدام الموارد
docker stats mt5-trading-bot

# حذف الـ container والبيانات
docker-compose down -v
```

### تحديث النظام

عندما تريد تحديث النظام ورفع نسخة جديدة:

```powershell
# على الجهاز الأصلي:
# 1. عدّل الكود
# 2. أعد بناء الصورة
docker-compose build

# 3. ارفع النسخة الجديدة
docker-compose push

# على الاستضافة:
# 1. أوقف النظام القديم
docker-compose down

# 2. اسحب النسخة الجديدة
docker-compose pull

# 3. شغّل النظام
docker-compose up -d
```

---

## 🔍 استكشاف الأخطاء

### المشكلة 1: Docker لا يعمل

```powershell
# تأكد من تشغيل Docker
Get-Service docker

# إعادة تشغيل Docker
Restart-Service docker
```

### المشكلة 2: لا يمكن الاتصال بـ MT5

**الحل:**
1. تأكد من صحة بيانات الاعتماد في `.env`
2. تأكد من أن MT5 مثبت ويعمل
3. تحقق من اتصال الإنترنت

```powershell
# مشاهدة اللوجات للتفاصيل
docker-compose logs -f
```

### المشكلة 3: الصورة كبيرة جداً

**الحل:**
- تأكد من وجود `.dockerignore` وأنه يستبعد الملفات الكبيرة
- لا ترفع ملفات ZIP أو بيانات تاريخية ضخمة

### المشكلة 4: خطأ في البناء

```powershell
# بناء جديد بدون cache
docker-compose build --no-cache

# حذف الصور القديمة
docker image prune -a
```

### المشكلة 5: الـ container يتوقف فوراً

```powershell
# مشاهدة سبب التوقف
docker logs mt5-trading-bot

# التحقق من ملف .env
cat .env
```

---

## 📊 مراقبة النظام

### عرض اللوجات

```powershell
# آخر 100 سطر
docker-compose logs --tail=100

# متابعة مباشرة
docker-compose logs -f

# لوجات من ملف داخل الـ container
docker exec mt5-trading-bot type C:\app\trading_system.log
```

### مراقبة الأداء

```powershell
# استخدام الموارد (CPU, RAM)
docker stats mt5-trading-bot
```

---

## 🎯 نصائح مهمة

1. **النسخ الاحتياطية**: احتفظ بنسخة من ملف `.env` في مكان آمن
2. **التحديثات الدورية**: حدّث Docker والمكتبات بانتظام
3. **الأمان**: لا تشارك ملف `.env` أو بيانات MT5
4. **المراقبة**: راقب اللوجات بانتظام للتأكد من عمل النظام
5. **الاختبار**: اختبر الصورة محلياً قبل رفعها
6. **التوثيق**: احتفظ بهذا الملف للرجوع إليه

---

## 📞 ملخص الأوامر السريعة

### على الجهاز الأصلي (أول مرة):
```powershell
# 1. إنشاء .env
Copy-Item .env.example .env
notepad .env  # عدّل البيانات

# 2. تسجيل دخول Docker Hub
docker login

# 3. بناء ورفع
docker-compose build
docker-compose push
```

### على الاستضافة الجديدة:
```powershell
# 1. إنشاء المجلد والملفات
mkdir C:\TradingSystem
cd C:\TradingSystem
# انسخ docker-compose.yml
# أنشئ .env بنفس البيانات

# 2. سحب وتشغيل
docker login
docker-compose pull
docker-compose up -d
```

---

## ✅ الخلاصة

الآن لديك نظام تداول كامل في Docker image يمكنك:
- ✅ رفعه إلى Docker Hub
- ✅ سحبه على أي استضافة Windows
- ✅ تشغيله بأوامر بسيطة
- ✅ نقله بسهولة بين الخوادم

**في حالة أي مشكلة في الاستضافة، فقط:**
1. ادخل إلى الاستضافة الجديدة
2. سجّل دخول Docker
3. اسحب الصورة: `docker-compose pull`
4. أنشئ `.env` بنفس البيانات
5. شغّل: `docker-compose up -d`

**الأمر كله لا يأخذ أكثر من 5 دقائق! 🚀**

</div>
