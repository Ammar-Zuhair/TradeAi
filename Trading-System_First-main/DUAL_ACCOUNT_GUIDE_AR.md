# دليل نظام الحسابين المزدوج 🔄

## نظرة عامة

هذا النظام يدير **حسابين MT5 بالتوازي** لمقارنة استراتيجيتين:

### 🔵 الحساب 1 - الاستراتيجية المتقدمة
```
✅ FVG قوي (score >= 60%)
✅ مع اتجاه H1 + M15  
✅ تنبؤ السعر يؤكد الاتجاه (إغلاق الشمعة الحالية)
✅ Volume & Momentum لا يعارضان
✅ 3+ أصوات في نفس الاتجاه
→ تنفيذ عند إعادة اختبار المنطقة
```

### 🟢 الحساب 2 - الاستراتيجية البسيطة  
```
✅ FVG قوي (score >= 60%)
✅ مع اتجاه H1 + M15 فقط
→ تنفيذ عند إعادة اختبار المنطقة (بدون شروط إضافية)
```

---

## 📋 الإعداد

### 1. تعديل ملف `.env`

```env
# الحساب 1 - الاستراتيجية المتقدمة
MT5_LOGIN_ADVANCED=12345678
MT5_PASSWORD_ADVANCED=YourPassword1
MT5_SERVER_ADVANCED=YourServer1

# الحساب 2 - الاستراتيجية البسيطة  
MT5_LOGIN_SIMPLE=87654321
MT5_PASSWORD_SIMPLE=YourPassword2
MT5_SERVER_SIMPLE=YourServer2

# التحكم
ENABLE_ADVANCED_STRATEGY=true
ENABLE_SIMPLE_STRATEGY=true
ENABLE_AUTO_TRADING=true
RISK_PERCENTAGE=1.0
```

### 2. التشغيل

```powershell
# مباشرة (بدون Docker)
python Run_System_Dual.py

# مع Docker
docker-compose up
```

---

## 🏗️ المعمارية

```
Container واحد → 3 Threads:

Thread 1: Data Updater
├─ يعمل كل 15 دقيقة
├─ يجلب بيانات FVG
└─ يحدث المناطق النشطة

Thread 2: Advanced Monitor 
├─ يراقب السعر كل ثانية
├─ يفحص الشروط المتقدمة
└─ ينفذ على الحساب 1

Thread 3: Simple Monitor
├─ يراقب السعر كل ثانية  
├─ يفحص الشروط البسيطة
└─ ينفذ على الحساب 2
```

---

## 🔍 آلية عمل الاستراتيجية

### عند كل ثانية:

1. **جلب السعر الحالي** (bid/ask)
2. **فحص المناطق النشطة**
3. **للمنطقة التي لم تُستخدم بعد:**

#### الاستراتيجية المتقدمة:
```python
if السعر في المنطقة (retest):
    if FVG قوي + مع الاتجاه:
        if تنبؤ السعر يؤكد:
            if Volume/Momentum لا يعارضان:
                if 3+ أصوات:
                    → تنفيذ الصفقة
                    → المنطقة تُحذف نهائياً
```

#### الاستراتيجية البسيطة:
```python
if السعر في المنطقة (retest):
    if FVG قوي + مع الاتجاه:
        → تنفيذ فوراً
        → المنطقة تُحذف نهائياً
```

---

## 📊 المناطق المستخدمة

**قاعدة مهمة:**
- ✅ عند تنفيذ صفقة على منطقة → تُحذف من الاهتمام
- ❌ المنطقة المحذوفة **لا تُعاد** حتى لو رجع السعر
- ✅ كل حساب له قائمة منفصلة للمناطق المستخدمة
- ✅ الحسابين لا يتداخلان

---

## 🎯 مثال عملي

**السيناريو:**
```
10:00 - Data Updater يكتشف FVG شرائي (score: 65%)
       - المنطقة: 2640-2642
       - الاتجاه: BUY (مع H1 + M15)
       
10:05 - السعر عند 2650 (فوق المنطقة)

10:12 - السعر ينزل إلى 2641 (داخل المنطقة) ← Retest!

الحساب البسيط (Simple):
→ ينفذ فوراً (كل الشروط موجودة)
→ يحذف المنطقة من قائمته

الحساب المتقدم (Advanced):
→ يفحص تنبؤ السعر → OK
→ يفحص Volume/Momentum → OK
→ يفحص التصويت → 4 أصوات BUY → OK
→ ينفذ الصفقة
→ يحذف المنطقة من قائمته

10:15 - السعر يعود إلى 2641 مرة أخرى
        → الحسابين يتجاهلان (المنطقة محذوفة)
```

---

## 📝 الملفات الناتجة

```
trading_advanced.log  - سجل الحساب المتقدم
trading_simple.log    - سجل الحساب البسيط
```

---

## ⚙️ التخصيص

### لتعطيل أحد الحسابين:

```env
ENABLE_ADVANCED_STRATEGY=false  # تعطيل المتقدم
ENABLE_SIMPLE_STRATEGY=true     # تفعيل البسيط فقط
```

### لتغيير سرعة الفحص:

```env
REALTIME_CHECK_SECONDS=2  # فحص كل ثانيتين بدلاً من ثانية
```

### لتغيير قوة FVG المطلوبة:

```env
FVG_STRONG_THRESHOLD=70  # قوي = 70% بدلاً من 60%
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Missing Advanced account MT5 credentials"

**الحل:**
```env
# تأكد من وجود هذه الأسطر في .env
MT5_LOGIN_ADVANCED=...
MT5_PASSWORD_ADVANCED=...
MT5_SERVER_ADVANCED=...
```

### المشكلة: "FVG file not found"

**الحل:**
- تأكد من وجود مجلد `detect_FVG`
- تأكد من وجود ملف `FVG_results.csv`
- شغّل تحليل FVG مرة واحدة أولاً

### المشكلة: الحسابين ينفذان نفس الصفقة

**هذا طبيعي!** كل حساب مستقل:
- الحساب البسيط ينفذ أسرع (شروط أقل)
- الحساب المتقدم ينفذ بعد الفحص الكامل
- كلاهما يستخدمان نفس المنطقة (للمقارنة)

---

## ✅ الخلاصة

- 🔄 نظام dual account للمقارنة
- ⚡ مراقبة لحظية (كل ثانية)
- 🎯 تنفيذ فوري عند retest
- 🔒 المناطق تُستخدم مرة واحدة فقط
- 📊 سجلات منفصلة لكل حساب
- 🐳 Docker-ready

**جاهز للمقارنة بين الاستراتيجيتين! 🚀**
