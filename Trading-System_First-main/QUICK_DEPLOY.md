# دليل النشر السريع - الاستضافة الجديدة 🚀

## 📦 التحضيرات (مرة واحدة فقط)

### 1. تنصيب Python 3.9
- حمّل من: https://www.python.org/ftp/python/3.9.13/python-3.9.13-amd64.exe
- **مهم:** اختر "Add Python to PATH"
- أكمل التنصيب

### 2. التحقق من التنصيب
```cmd
python --version
pip --version
```

---

## 🔄 كل 6 ساعات (بعد انتهاء الاستضافة)

### الطريقة السريعة (موصى بها):

```cmd
1. نسخ المجلد كامل للاستضافة الجديدة
   (أو تحميل من GitHub/Drive)

2. داخل المجلد، double-click على:
   start.bat

3. انتهى! ✅
```

---

### الطريقة اليدوية:

```cmd
REM 1. الانتقال للمجلد
cd C:\path\to\Final_project

REM 2. تنصيب المكتبات (دقيقتين)
pip install -r requirements.txt

REM 3. التأكد من وجود .env
REM (نسخ من .env.example وتعديل البيانات)

REM 4. تشغيل
python Run_System_Dual.py
```

---

## 📁 الملفات المطلوبة على الاستضافة

### الأساسية (لا بد منها):
```
Final_project/
├── Run_System_Dual.py       ✅ الملف الرئيسي
├── env_loader.py             ✅ تحميل الإعدادات
├── requirements.txt          ✅ المكتبات
├── .env                      ✅ بياناتك (أنشئه!)
├── start.bat                 ✅ التشغيل السريع
│
├── detect_FVG/               ✅ نظام FVG
├── PredictNextPrice/         ✅ نظام التنبؤ
├── models/                   ✅ النماذج
├── scalers/                  ✅ المعايرة
└── getDataAndVoting.py       ✅ التصويت
```

### ملفات البيانات (اختيارية - ستُنشأ تلقائياً):
```
logs/                         📝 ملفات اللوج
data/                         💾 البيانات
detect_FVG/*.csv              📊 بيانات FVG
```

---

## ⚙️ ملف `.env` (مهم جداً!)

قبل التشغيل، أنشئ ملف `.env`:

```env
# الحساب المتقدم
MT5_LOGIN_ADVANCED=197968807
MT5_PASSWORD_ADVANCED=YourPassword1
MT5_SERVER_ADVANCED=YourServer1

# الحساب البسيط
MT5_LOGIN_SIMPLE=197968970
MT5_PASSWORD_SIMPLE=YourPassword2  
MT5_SERVER_SIMPLE=YourServer2

# الإعدادات
ENABLE_ADVANCED_STRATEGY=true
ENABLE_SIMPLE_STRATEGY=true
ENABLE_AUTO_TRADING=false
RISK_PERCENTAGE=1.0
SYMBOL=XAUUSDm
CHECK_INTERVAL_MINUTES=15
REALTIME_CHECK_SECONDS=1
```

---

## 🎯 سيناريو النشر الكامل

### على جهازك (الآن):
```cmd
1. ضغط المجلد Final_project
2. رفع للـ Google Drive / OneDrive
   (أو push لـ GitHub private repo)
```

### على الاستضافة الجديدة:
```cmd
1. تحميل المجلد المضغوط
2. فك الضغط
3. تعديل .env (البيانات الحقيقية)
4. Double-click: start.bat
5. انتظر دقيقتين → النظام يعمل! ✅
```

---

## 🔍 التحقق من النجاح

بعد التشغيل، سترى:
```
✅ Advanced account validated
✅ Simple account validated
🚀 Starting 3 threads...
✅ MT5 initialized
📊 Account 197968807 | Balance: $XXX
📊 Account 197968970 | Balance: $XXX
✅ System running!
```

---

## 🆘 استكشاف الأخطاء

### خطأ: "Python not found"
```cmd
# إعادة تنصيب Python مع "Add to PATH"
```

### خطأ: "MetaTrader5 connection failed"
```cmd
# تأكد من:
1. MT5 مُنصّب ومشغّل
2. البيانات في .env صحيحة
3. الحساب Demo/Live نشط
```

### خطأ: "Symbol XAUUSDm not available"
```cmd
# في MT5:
1. Market Watch → Show All
2. ابحث عن XAUUSD أو XAUUSDm
3. Add to Market Watch
```

---

## ⏱️ الوقت المتوقع

| الخطوة | الوقت |
|--------|-------|
| نسخ/تحميل المجلد | 1-2 دقيقة |
| تنصيب المكتبات | 2-3 دقائق |
| تعديل .env | 1 دقيقة |
| التشغيل | فوري |
| **المجموع** | **5 دقائق** ⚡ |

---

## 💾 نسخة احتياطية (Backup)

احتفظ بنسخة من:
- ✅ المجلد كامل (مضغوط)
- ✅ ملف `.env` (منفصل - آمن)
- ✅ على Drive/GitHub

---

## 🎉 الخلاصة

**بدلاً من Docker (معقد):**
```
✅ نسخ المجلد
✅ تشغيل start.bat
✅ انتهى!
```

**بسيط - سريع - يعمل!** 🚀
